# Uncomment the next line to define a global platform for your project
# platform :ios, '11.0'

target 'RecyclingStarter' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_modular_headers!
  
  pod 'Alamofire'
  pod 'TinyConstraints'
  pod 'SwiftRichString'

  # Pods for RecyclingStarter

end
